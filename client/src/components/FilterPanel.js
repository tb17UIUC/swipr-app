import React from 'react';
import { useState, useEffect } from 'react';

export default function FilterPanel({ brands, universities, types }) {
    const [filters, setFilters] = {};

    useEffect(() => {}, [filters]);

    return <div></div>;
}
